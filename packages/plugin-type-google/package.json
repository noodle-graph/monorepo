{
    "name": "@noodle-graph/plugin-type-google",
    "version": "0.0.0",
    "scripts": {
        "build": "tsc --project tsconfig.lib.json && cp package.json dist && cp README.md dist",
        "lint": "eslint .",
        "test": "jest"
    },
    "devDependencies": {
        "@noodle-graph/types": "file:../types/dist"
    },
    "engines": {
        "node": ">=16"
    }
}
