{
    "name": "@noodle-graph/cli",
    "version": "0.0.0",
    "scripts": {
        "build": "nx build ui && copy-files && tsc --project tsconfig.lib.json && cp package.json dist",
        "lint": "eslint .",
        "test": "jest"
    },
    "devDependencies": {},
    "dependencies": {},
    "engines": {
        "node": ">=16"
    }
}
